- check target cell text content with numbers array index index (if num === 0) continue
- sounds on click
- check index:-1, +1, -arr.length, arr.length

Не выполненные задачи:
1) при клике по фишке, стоящей рядом с пустой клеткой, фишка перемещается на место пустой клетки

2) фишки можно перетягивать мышкой

3) реализовано сохранение состояния игры и сохранение 10 лучших результатов с иcпользованием LocalStorage

4) реализован выбор размера поля

5) анимация перемещения пятнашек на поле

6) когда игра закончилась, выводится сообщение «Ура! Вы решили головоломку за ##:## и N ходов»

7) вместо цифр на карточках используются картинки

8) реализовано автоматическое, анимированное завершение игры